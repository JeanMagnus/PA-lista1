#include <stdio.h>

int main(int argc, char const *argv[])
{
    /*
        *(pulo + 2);
        *(pulo + 4);
        pulo + 4;
        pulo + 2;
        a expressão *(pulo) referencia o valor do primeiro elemento, 
        ja a expressão *(pulo + 2); anda dois endereços para frente e 
        referencia o valor do terceiro elemento
    */
    return 0;
}